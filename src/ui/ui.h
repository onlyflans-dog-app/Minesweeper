#pragma once

#include "_include.h"

extern int uidebug;

enum gameMode{
// general menu
    GAME_MODE_STARTSCREEN,
    GAME_MODE_MAIN,

    GAME_MODE_NEWGAME,
    GAME_MODE_GAME,
    GAME_MODE_WIN,

    GAME_MODE_SETTINGS,
    GAME_MODE_ABOUT,
    GAME_MODE_CREDITSCREEN,

// game/soft specific
};